# 复制本文件为 terraform.tfvars，然后填入你自己的密码
#   cp terraform.tfvars.example terraform.tfvars
#
# terraform.tfvars 已被 .gitignore 排除，不会提交进仓库
# AK / SK 请走环境变量：ALICLOUD_ACCESS_KEY / ALICLOUD_SECRET_KEY

ecs_password = "REPLACE_ME"
