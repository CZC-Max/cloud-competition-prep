# ---------- Terraform ----------
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
.terraformrc
terraform.rc

# ---------- 变量文件（可能含密码）----------
# tfstate 与 tfvars 里都可能有明文密钥，绝不入库
*.tfvars
!*.tfvars.example
*.auto.tfvars

# ---------- 凭证 / 密钥 ----------
.env
.env.*
*.pem
*.key
*.p12
config.json
credentials
.accesskey*
id_rsa
id_rsa.pub

# ---------- 编辑器 / 系统 ----------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
