# 云实验记录模板

> 沿用 CTF 总结的「现象 → 思路 → 步骤 → 经验」四段式，改成云实验版。
> 每练完一个模块，复制本模板写一份，**300-800 字即可**。
> 不追求文采，只追求一个标准：**别人搜到这个问题，能照着做出来**。

---

## <实验标题>

**日期**：YYYY-MM-DD
**耗时**：xx 分钟
**环境**：阿里云 cn-hangzhou / 可用区 k / 按量付费
**花费**：约 x 元（已 destroy）

### 一、目标

一句话说清这次要建成什么，或要排掉什么故障。

### 二、现象

遇到了什么报错或异常？**贴关键报错原文**，不要只写"失败了"。

```
<报错原文粘贴在这里>
```

### 三、思路

为什么会出现？列出判断链条（由外到内 / 由下到上逐层收窄）。

### 四、步骤

可复现的操作，命令要能直接复制执行。

```bash
terraform plan
terraform apply
```

### 五、结果

怎么确认成功了？（浏览器截图 / 命令输出 / 控制台状态）

### 六、经验

- 这次学到的一句话结论
- 下次要避开的坑

### 七、清理（必做，别省）

- [ ] `terraform destroy` 已执行
- [ ] `terraform state list` 返回空
- [ ] 控制台确认资源真的消失（三看已完成）

---

**发布**：写完存进 `docs/`，或直接作为博客文章发出去。
